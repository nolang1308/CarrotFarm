/**
 * 지금 화면에 실제로 그려지는 밭의 오프셋(애니메이션 중간값).
 * 관계식: 타일의 월드 좌표 = 그리드 좌표 + farmOffset
 *
 * Farm 이 매 프레임 이 값을 갱신하고, BuildLayer(좌표 변환·고스트)가 읽어
 * 애니메이션 중에도 밭·미리보기·마우스 판정이 서로 어긋나지 않게 한다.
 */
export const farmOffset = { x: 0, z: 0 }
